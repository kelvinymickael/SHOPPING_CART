import { createContext } from "react";

// Criando o nosso contexto utilizando a função (createContext):
const AppContext = createContext();

// Exportando o meu contexto criado para utilizar em outros arquivos:
export default AppContext;
